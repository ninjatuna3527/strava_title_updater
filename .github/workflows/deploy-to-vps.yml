name: Deploy to VPS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    environment: prod
    runs-on: ubuntu-latest
    env:
      VPS_HOST: ${{ secrets.VPS_HOST }}
      VPS_USER: ${{ secrets.VPS_USER }}
      VPS_PORT: ${{ secrets.VPS_PORT }}
      DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
      BASE_PATH: ${{ secrets.BASE_PATH }}
      DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
      SUPERVISOR_PROGRAM: ${{ secrets.SUPERVISOR_PROGRAM }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.VPS_PRIVATE_KEY }}

      - name: Install Ansible
        run: |
          python3 -m pip install --upgrade pip
          pip install ansible

      - name: Add VPS to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p ${{ secrets.VPS_PORT }} ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Run Ansible playbook (prod inventory)
        env:
          DEPLOY_PATH: ${{ vars.DEPLOY_PATH }}
          SUPERVISOR_PROGRAM: ${{ vars.SUPERVISOR_PROGRAM }}
          SUPERVISOR_SCHEDULER_PROGRAM: ${{ vars.SUPERVISOR_SCHEDULER_PROGRAM }}
          BASE_PATH: ${{ vars.BASE_PATH }}
          STRAVA_CLIENT_ID: ${{ secrets.STRAVA_CLIENT_ID }}
          STRAVA_CLIENT_SECRET: ${{ secrets.STRAVA_CLIENT_SECRET }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ vars.OPENAI_MODEL }}
          CALLBACK_HOSTNAME: ${{ vars.CALLBACK_HOSTNAME }}
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          FLASK_SECRET_KEY: ${{ secrets.FLASK_SECRET_KEY }}
          DB_PATH: ${{ vars.DB_PATH }}
          NGINX_RATE_LIMIT_ZONE_SIZE: ${{ vars.NGINX_RATE_LIMIT_ZONE_SIZE }}
          NGINX_RATE_LIMIT_RATE: ${{ vars.NGINX_RATE_LIMIT_RATE }}
          NGINX_RATE_LIMIT_BURST: ${{ vars.NGINX_RATE_LIMIT_BURST }}
          SMTP_SENDER: ${{ vars.SMTP_SENDER }}
          SMTP_USER: ${{ vars.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          SMTP_HOST: ${{ vars.SMTP_HOST }}
          SMTP_PORT: ${{ vars.SMTP_PORT }}
          SMTP_SECURITY: ${{ vars.SMTP_SECURITY }}
          ADMIN_EMAIL: ${{ vars.ADMIN_EMAIL }}
        run: |
          ansible-playbook deploy/ansible/playbook.yml -i deploy/ansible/inventories/prod/inventory \
            --extra-vars "deploy_path=${DEPLOY_PATH} supervisor_program=${SUPERVISOR_PROGRAM} scheduler_supervisor_program=${SUPERVISOR_SCHEDULER_PROGRAM} repo_branch=main base_path=${BASE_PATH} db_path=${DB_PATH} nginx_rate_limit_zone_size=${NGINX_RATE_LIMIT_ZONE_SIZE} nginx_rate_limit_rate=${NGINX_RATE_LIMIT_RATE} nginx_rate_limit_burst=${NGINX_RATE_LIMIT_BURST}"
